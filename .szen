### Information about the Space Language

- Keywords:
    and        break      continue   define
    else       if         in         import
    is         or         let        not
    return     True       False      None

- Identifier:
    [a-zA-Z][a-zA-Z0-9_ ']*[a-zA-Z0-9]$

- Escape:
    \0, \a, \b, \f, \n, \r, \s, \t, \v
    \\, \", \', \{, \},
    \x[a-fA-F0-9]{2}, \u{1,8}

- Special:
    @inline
    @static
    @public

    Combine: @static.inline.public
    # Example
        @static let x = 10

        @static
        let x = 10

        @static.inline
        let x = 10;
