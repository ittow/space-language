### Information about the Space Language

- Keywords:
    as        and       [async]   [await]   break
    continue  define    else      if        import
    let       not       or        return     True
    False

    - remove:
    async     await

    - extend:
    immutable mutable   static    unsafe

- Identifier:
    [a-zA-Z][a-zA-Z0-9 ']*[a-zA-Z0-9]$

- Escape:
    \0, \a, \b, \f, \n, \r, \s, \t, \v
    \\, \", \', \{, \},
    \x[a-fA-F0-9]{2}, \u[a-fA-F0-9]{1,8}

- Operator:
    +    -    *    /
    %    ^^   |    &
    ~    <    >    =
    ^    <=   >=   !=
    <<   >>

- Special:
    @inline
    @static
    @rules
    @load
    @type
